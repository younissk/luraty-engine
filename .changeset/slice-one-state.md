---
'@luraty/engine': minor
---

Add the learner state: `Profile`, `UnitState`, `Evidence`, and `record()`.

Knowledge is addressed per `direction:variety:word`, so recognising a word and being able to
produce it are separate states, and a dialect is measured separately from the standard. Only a real
retrieval can promote a unit — a passive signal counts as an encounter and nothing more.
